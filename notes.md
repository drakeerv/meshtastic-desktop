Remaining notes:

- Make sure to doc comment everything ig.
- Make logging better/more thorough?
- Add a tray icon/process for background usage/notifications?

If we are not already doing it:
- alloww host to use geoclue or other geolocation provider and pass it to device
- allow host to fill out the timezone in settings with a "Fill from host" button (Android has it)
- allow us to set the devices time from host on startup/link whatever
